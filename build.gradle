plugins {
    id 'java'
    id 'application'
    id 'org.graalvm.buildtools.native' version '0.9.11'
}

group 'ru.workshop'
version '3.0-SNAPSHOT'

repositories {
    mavenCentral()
}

ext {
    javaMainClass = "ru.workshop.App"
}

application {
    mainClassName = javaMainClass
}

jar {
    manifest {
        attributes(
                "Main-Class": javaMainClass
        )
    }
}

graalvmNative {
    toolchainDetection = false

    useArgFile = false
    binaries {
        main {
            useFatJar = true
            mainClass = javaMainClass
        }
    }
}